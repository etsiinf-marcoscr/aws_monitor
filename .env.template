AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_key
AWS_SESSION_TOKEN=tu_session_token (opcional)
AWS_DEFAULT_REGION=us-east-1

# Por defecto en 'true' para no gastar crédito usando datos simulados. Cambia a 'false' para usar datos reales de AWS.
MOCK_MODE=true

# mapbox.com
MAPBOX_TOKEN=tu_mapbox_token (opcional, solo necesario para la página de mapa)
